--- a/DRAMALESS_SHAPE-2.0.1/lib/VoxelScene.lua
+++ b/DRAMALESS_SHAPE-2.0.1/lib/VoxelScene.lua
@@ -91,7 +91,16 @@
 local function withinRenderDistance(subject)
   local state = overworld()
   local ow_player = state.player
-  local me = {px = ow_player.cellX, py = ow_player.cellY}
+  -- `subject.px/py` are world-pixel coordinates. Player.cellX/cellY are
+  -- movement-grid cells (16 px each), so comparing them directly makes
+  -- render-distance culling depend on absolute map position: nearby NPCs
+  -- and follower Pokemon can vanish after crossing an X/Y threshold.
+  -- Use the engine's authoritative pixel position, with a cell fallback
+  -- for compatibility with custom player objects.
+  local me = {
+    px = tonumber(ow_player.px) or ((tonumber(ow_player.cellX) or 0) * 16),
+    py = tonumber(ow_player.py) or ((tonumber(ow_player.cellY) or 0) * 16),
+  }
   if not (subject.isPlayer) then
     local mx = me.px
     local my = me.py
--- a/DRAMALESS_SHAPE-2.0.1/manifest.json
+++ b/DRAMALESS_SHAPE-2.0.1/manifest.json
@@ -1,8 +1,7 @@
 {
-  "id": "DRAMALESS_SHAPE"
-,
+  "id": "DRAMALESS_SHAPE",
   "name": "Dramaless Shape Voxel Mod",
-  "version": "2.0.1",
+  "version": "2.0.1-sandbox.2",
   "api": 2,
   "entry": "main.lua",
   "profile": "content",
@@ -26,6 +25,6 @@
   ],
   "affects_link": false,
   "experimental": false,
-  "description": "Voxel environments for Gen1Recomp, with standalone native-2D voxel battles and optional StadiumBattleFX providers.",
+  "description": "Voxel environments for Gen1Recomp, with Android render-distance coordinate fix and ROM-safe mod packaging.",
   "github": "artyrambles/DRAMALESS_SHAPE"
 }
